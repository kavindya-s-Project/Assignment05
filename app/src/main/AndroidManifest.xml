<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.assignment05">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Assignment05">
        <activity android:name=".ContactUs"></activity>
        <activity android:name=".AboutUs" />
        <activity android:name=".FindLocation" />
        <activity android:name=".SuccessStories" />
        <activity android:name=".Donations" />
        <activity
            android:name=".ui.login.Donations"
            android:label="@string/title_activity_donations" />
        <activity android:name=".NewsAndEvents" />
        <activity android:name=".LatestPosts" />
        <activity android:name=".LoginActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>